services:
  resume-site:
    image: nginx:alpine
    container_name: my_cv_static
    restart: always
    ports:
      - "8880:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
    networks:
      - cv-network

networks:
  cv-network:
    driver: bridge